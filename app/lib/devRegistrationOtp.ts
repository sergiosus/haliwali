/**
 * Local registration OTP helpers for developers live in `/api/auth/request-registration-code`:
 * when `NODE_ENV !== "production"`, the handler returns `{ devCode }` and logs the code server-side only.
 */

export {};
