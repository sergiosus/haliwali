/** /marketplaces page timing and fetch limits. */
export const MARKETPLACE_PREVIEW_MAX_PER_PROVIDER = 3;
export const MARKETPLACE_PREVIEW_MAX_TOTAL = 8;
/** @deprecated Use preview limits for /marketplaces live cards. */
export const MARKETPLACE_PAGE_MAX_PER_PROVIDER = MARKETPLACE_PREVIEW_MAX_PER_PROVIDER;
/** @deprecated Use preview limits for /marketplaces live cards. */
export const MARKETPLACE_PAGE_MAX_TOTAL = MARKETPLACE_PREVIEW_MAX_TOTAL;
export const MARKETPLACE_PAGE_SEARCH_DEBOUNCE_MS = 280;
